# requirements-multimodal.txt
# Additional dependencies for the multimodal extraction + sessions +
# guardrails + observability layer (app/extraction, app/chunking,
# app/sessions, app/guardrails, app/observability, app/api_v2).
#
# Install alongside the existing requirements.txt:
#   pip install -r requirements.txt -r requirements-multimodal.txt
#
# Does not modify or replace requirements.txt.

# --- PDF: tables + OCR (pymupdf/fitz already in requirements.txt) ---
pdfplumber>=0.10
pytesseract>=0.3.10
Pillow>=10.0

# --- DOCX / PPTX / XLSX extraction ---
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1

# --- API layer (upload_routes.py multipart handling) ---
python-multipart>=0.0.9

# --- UI (streamlit_app_v2.py talks to the API over HTTP) ---
requests>=2.31